apiVersion: 1

providers:
  - name: job-queue
    type: file
    disableDeletion: false # можно удалять дашборды через UI
    editable: true # можно редактировать через UI
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
